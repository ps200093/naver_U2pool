# 프로젝트 구조 및 파일 설명

## 📂 디렉토리 구조

```
naver_U2pool/
│
├── 📁 src/                          # 소스 코드
│   ├── __init__.py                 # 패키지 초기화
│   ├── crawler.py                  # 메인 크롤러 (모바일/데스크톱)
│   ├── mobile_driver.py            # ⭐ 모바일 에뮬레이션 핵심
│   └── utils.py                    # 유틸리티 함수 (JSON/CSV 저장)
│
├── 📁 config/                       # 설정 파일
│   └── config.json                 # 크롤러 설정
│
├── 📁 examples/                     # 사용 예제
│   ├── simple_example.py           # 기본 사용법
│   └── advanced_example.py         # 고급 기능
│
├── 📁 docs/                         # 문서
│   ├── MOBILE_EMULATION_GUIDE.md   # 모바일 에뮬레이션 가이드
│   └── QUICKSTART.md               # 빠른 시작 가이드
│
├── 📁 drivers/                      # ChromeDriver
│   └── chromedriver.exe            # Windows용 드라이버
│
├── 📁 logs/                         # 로그 (자동 생성)
│   └── crawler.log
│
├── 📁 data/                         # 크롤링 데이터 (자동 생성)
│   ├── *.json                      # JSON 데이터
│   ├── *.csv                       # CSV 데이터
│   └── screenshots/                # 스크린샷
│
├── 📁 venv/                         # 가상환경
│
├── 📄 main.py                      # 메인 실행 파일
├── 📄 test_mobile.py               # 모바일 테스트 스크립트
├── 📄 requirements.txt             # 패키지 목록
├── 📄 .gitignore                   # Git 제외 파일
├── 📄 README.md                    # 프로젝트 메인 문서
└── 📄 PROJECT_STRUCTURE.md         # 이 파일
```

## 📝 주요 파일 설명

### 🎯 핵심 파일

#### `src/mobile_driver.py` ⭐
**완벽한 모바일 에뮬레이션의 핵심**

**기능:**
- 4가지 모바일 기기 프리셋 (Galaxy S24, S23, iPhone 15 Pro, 14)
- Chrome 버전 자동 감지 및 동기화
- CDP를 통한 저수준 브라우저 제어
- JavaScript 주입으로 자동화 감지 방지
- 완벽한 Navigator 속성 위장

**클래스:**
```python
class MobileDriver:
    def __init__(driver_path, headless, device)
    def create_driver() -> webdriver.Chrome
    def _apply_mobile_settings(driver)
    def _inject_mobile_javascript(driver)
    def _verify_mobile_settings(driver)
```

**사용 예:**
```python
mobile = MobileDriver(headless=False, device="galaxy_s24")
driver = mobile.create_driver()
driver.get("https://m.naver.com")
```

#### `src/crawler.py`
**사용하기 쉬운 크롤러 래퍼**

**기능:**
- MobileDriver 통합
- 컨텍스트 매니저 지원
- 로깅 자동 설정
- 페이지 대기 및 요소 대기

**클래스:**
```python
class NaverCrawler:
    def __init__(driver_path, headless, use_mobile, device)
    def setup_driver()
    def get_page(url, wait_time)
    def wait_for_element(by, value, timeout)
    def close()
```

**사용 예:**
```python
with NaverCrawler(use_mobile=True, device="galaxy_s24") as crawler:
    crawler.get_page("https://m.naver.com")
```

#### `src/utils.py`
**유틸리티 함수 모음**

**기능:**
- JSON 데이터 저장
- CSV 데이터 저장 (Excel 호환)
- 설정 파일 로드/저장
- 자동 타임스탬프

**함수:**
```python
def save_to_json(data, filename)
def save_to_csv(data, filename, fieldnames)
def load_config(config_file)
def save_config(config, config_file)
```

### 🚀 실행 파일

#### `main.py`
**기본 실행 파일**

- 모바일 모드로 네이버 접속
- User Agent 확인
- 대화형 실행

#### `test_mobile.py`
**모바일 드라이버 테스트**

**테스트 항목:**
- 단일 기기 테스트
- 모든 기기 순차 테스트
- 대화형 테스트 모드
- 자동 검증

**함수:**
```python
def test_single_device(device_name)
def test_all_devices()
def interactive_test()
```

### 📚 예제 파일

#### `examples/simple_example.py`
**기본 사용 예제**

**포함 내용:**
- 네이버 모바일 검색
- 네이버 쇼핑 둘러보기
- 여러 기기로 비교
- 대화형 메뉴

#### `examples/advanced_example.py`
**고급 기능 예제**

**포함 내용:**
- Headless 모드 크롤링
- 스크린샷 저장
- 쿠키 관리
- 페이지 정보 추출
- 모바일 vs 데스크톱 비교

### ⚙️ 설정 파일

#### `config/config.json`
**크롤러 설정**

```json
{
  "driver_path": "",           // ChromeDriver 경로 (비우면 자동)
  "headless": false,           // Headless 모드
  "wait_time": 3,             // 페이지 로드 대기
  "timeout": 10,              // 요소 대기 최대 시간
  "use_mobile": true,         // 모바일 에뮬레이션
  "device": "galaxy_s24",     // 기기 선택
  "mobile_devices": {...}     // 기기 정보
}
```

### 📖 문서 파일

#### `docs/MOBILE_EMULATION_GUIDE.md`
**완벽한 모바일 에뮬레이션 가이드**

**내용:**
- 핵심 기술 설명
- CDP 사용법
- JavaScript 주입
- 검증 방법
- 지원 기기 목록
- 고급 기능
- 문제 해결

#### `docs/QUICKSTART.md`
**5분 빠른 시작 가이드**

**내용:**
- 단계별 설치
- 첫 실행
- 명령어 치트시트
- FAQ
- 문제 해결

#### `README.md`
**프로젝트 메인 문서**

**내용:**
- 프로젝트 소개
- 설치 및 실행
- 사용 예시
- 주요 기능
- 검증 방법

## 🔄 데이터 플로우

```
1. 사용자 실행
   ↓
2. NaverCrawler 초기화
   ↓
3. MobileDriver 생성
   ↓
4. Chrome 버전 감지
   ↓
5. Chrome Options 설정
   ↓
6. WebDriver 생성
   ↓
7. CDP 명령 실행 (모바일 설정)
   ↓
8. JavaScript 주입 (위장)
   ↓
9. 설정 검증
   ↓
10. 크롤링 수행
    ↓
11. 데이터 저장 (utils.py)
    ↓
12. 드라이버 종료
```

## 🎨 모바일 에뮬레이션 레이어

```
┌─────────────────────────────────────┐
│  User Code (main.py, examples/)     │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│  NaverCrawler (crawler.py)          │
│  - 컨텍스트 매니저                    │
│  - 로깅                              │
│  - 페이지 대기                        │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│  MobileDriver (mobile_driver.py)    │
│  ┌─────────────────────────────┐   │
│  │ 1. Chrome Options           │   │
│  │    - User Agent             │   │
│  │    - Mobile Emulation       │   │
│  │    - Touch Events           │   │
│  └─────────────────────────────┘   │
│  ┌─────────────────────────────┐   │
│  │ 2. CDP Commands             │   │
│  │    - Navigator Override     │   │
│  │    - Touch Emulation        │   │
│  │    - Device Metrics         │   │
│  │    - User Agent Metadata    │   │
│  └─────────────────────────────┘   │
│  ┌─────────────────────────────┐   │
│  │ 3. JavaScript Injection     │   │
│  │    - Navigator Proxy        │   │
│  │    - Touch Events           │   │
│  │    - matchMedia             │   │
│  │    - Automation Detection   │   │
│  └─────────────────────────────┘   │
│  ┌─────────────────────────────┐   │
│  │ 4. Verification             │   │
│  │    - Platform Check         │   │
│  │    - Mobile Detection       │   │
│  │    - Touch Points           │   │
│  └─────────────────────────────┘   │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│  Selenium WebDriver                 │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│  Chrome Browser (Headless/Normal)   │
└─────────────────────────────────────┘
```

## 🔍 파일별 책임

| 파일 | 책임 | 의존성 |
|------|------|--------|
| `mobile_driver.py` | 모바일 에뮬레이션 핵심 로직 | selenium |
| `crawler.py` | 크롤러 래퍼, 사용성 | mobile_driver.py |
| `utils.py` | 데이터 저장 유틸리티 | - |
| `main.py` | 메인 실행 | crawler.py, utils.py |
| `test_mobile.py` | 테스트 스크립트 | mobile_driver.py |
| `examples/` | 사용 예제 | crawler.py, utils.py |

## 📦 의존성 그래프

```
requirements.txt
    │
    ├── selenium (핵심)
    ├── webdriver-manager (선택)
    └── python-dotenv (선택)

src/mobile_driver.py
    ├── selenium.webdriver
    ├── selenium.webdriver.chrome.service
    └── selenium.webdriver.chrome.options

src/crawler.py
    ├── src.mobile_driver
    └── selenium.webdriver.support

src/utils.py
    ├── json (내장)
    ├── csv (내장)
    └── pathlib (내장)

main.py
    ├── src.crawler
    └── src.utils
```

## 🎯 핵심 개념

### 1. 컨텍스트 매니저
```python
with NaverCrawler(...) as crawler:
    # 자동으로 setup_driver() 호출
    crawler.get_page(...)
    # 자동으로 close() 호출
```

### 2. 프리셋 기반 설정
```python
DEVICES = {
    "galaxy_s24": {...},  # 미리 정의된 설정
    "iphone_15_pro": {...}
}
```

### 3. 3단계 위장
- **Level 1**: Chrome Options (기본)
- **Level 2**: CDP Commands (고급)
- **Level 3**: JavaScript Injection (완벽)

### 4. 자동 검증
- 모든 드라이버 생성 시 자동으로 설정 검증
- 로그로 결과 출력
- 문제 발견 시 경고

## 🚀 확장 가능성

### 새로운 기기 추가
`src/mobile_driver.py`의 `DEVICES`에 추가:
```python
"new_device": {
    "user_agent": "...",
    "width": 360,
    "height": 800,
    # ...
}
```

### 새로운 기능 추가
1. `src/utils.py`에 유틸리티 함수 추가
2. `src/crawler.py`에 크롤러 메서드 추가
3. `examples/`에 예제 추가

### 프록시 지원 (준비됨)
```python
# mobile_driver.py에 이미 프록시 설정 코드 포함
# proxy_config 파라미터 사용 가능
```

---

**프로젝트**: naver_U2pool  
**버전**: 1.0  
**최종 업데이트**: 2025-11-18

